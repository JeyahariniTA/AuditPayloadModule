server:
    port: 8093
spring:
    application:
        name: AuditPayloadModule
    data:
        mongodb:
            uri: mongodb://localhost:27017/TestDB
            database: TestDB
            auto-index-creation: true
    jpa:
        hibernate:
            ddl-auto: update
logging:
    level:
        org:
            springframework:
                data:
                    mongodb:
                        core:
                            MongoTemplate: DEBUG